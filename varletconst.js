var genty="male"

function getGender() {
    if(genty.startsWith("male")) {
        var age=30 
            let color= "red"

        console.log(" colour is: " + color);
    } else {   
        let color= "blue"
        var age=25
    
        console.log(" colour is: " + color);
    }  
    console.log("Age is: " + age); 
}
    getGender()
